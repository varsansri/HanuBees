# Progress — 2026-06-06

Full record of today's marketing work, changes, and edits.

## Highlights

- **Published the first 5-slide data-viz STORY carousel** (Los Angeles, real data) to
  **Instagram + TikTok** — both `PUBLISHED ✓`.
- **Turned the story generator into a truthful, self-driving system**: every story is built
  live from the database, auto-picks the next un-posted AU/US city, and logs itself.
- **Organized all marketing into this `hanubees-marketing-team/` folder** (scripts, assets, docs, credentials).
- **Scheduled a daily auto-poster cron** (10:23 AM) to drip one city's story per day.

## What shipped

### 1. Story carousel finished & verified
- Slides: Cover (emotion) → Big stat + donut → Bar chart → Flowchart (old vs Hanubees) → CTA.
- Switched all image text to the **brand font Space Grotesk** (fetched to `assets/brand/fonts/SpaceGrotesk.ttf`;
  `setupFonts()` points fontconfig at it and maps sans-serif/Arial → Space Grotesk).
- Fixed layout bugs found during visual verification:
  - Slide 3 (bars): bars start moved `y0 360 → 470` so the title no longer overlaps the first bar.
  - Slide 4 (flow): boxes/labels shifted down (old `330→430`, new `470→560/600→690`, labels to 400/530)
    so content clears the title.
  - Shortened titles: `barsTitle → "Across N <City> businesses"`, `flowTitle → "How customers reach a business today"`.
- Every slide visually re-checked via image read; bee.png composited on each.

### 2. Generator made data-driven + self-driving (`story-carousel.js`)
- New `buildStory(row)` constructs an **all-true** story from a city's live counts
  (total / no-phone / no-website / reachable / %). No hardcoded stats.
- New `nextCity()` picks the next **AU/US target city** with ≥40 businesses, preferring
  never-posted then oldest-posted.
- New modes: `auto` (post next city), `render auto` (preview), `<City>` (specific city).
- Per-platform captions kept (IG long, TikTok ≤90 chars).
- Confirmed working after relocation: `render auto` → Melbourne (76/151 = 50% unreachable).

### 3. New DB table
```sql
create table story_log (
  id uuid primary key default gen_random_uuid(),
  city text, slides int, platforms text, posted_at timestamptz default now()
);
```
Seeded with `Los Angeles` (already posted) so `auto` skips it; **Melbourne is next**.

### 4. Folder reorg → `hanubees-marketing-team/`
- Moved brand assets `assets/brand/` → `hanubees-marketing-team/assets/brand/`.
- Moved social scripts (`story-carousel`, `post-carousel`, `post-instagram`, `gen-social`,
  `social-post`, `social.cron.yml.txt`) → `hanubees-marketing-team/scripts/`.
- Updated in-script asset paths to the new location; verified scripts still run from project root.
- Data-ingest scripts + `db.js` stayed in `scripts/` (shared infra, not marketing-only).
- Added docs: README, CREDENTIALS (masked inventory), SOCIAL-SYSTEM, SEO, this PROGRESS.

### 5. Daily cron
- Session cron `b116b023` at `23 10 * * *` runs
  `node hanubees-marketing-team/scripts/story-carousel.js auto`.
- Caveats: runs while the session is alive; auto-expires after 7 days; no system cron here.

## TikTok carousel confirmation
TikTok **Photo Mode** supports multi-image carousels (up to 35), so the same 5 slides post to
both IG and TikTok. Only difference: TikTok caption is capped at 90 chars (handled by `caption_tt`).

## Open tasks
1. **Ingest more AU/US cities** (Sydney, Brisbane, Perth, SF, NYC, Chicago…) — each becomes one
   genuine story; the real lever to reach ~40 posts. `TARGETS` list already wired.
2. **True 24/7 posting**: port the renderer to a **Vercel cron API route** (bundle Space Grotesk
   + add `sharp` as a dependency) so it doesn't depend on the local session.
3. **SEO #1 blocker**: set `NEXT_PUBLIC_GOOGLE_VERIFICATION` (Google Search Console token) in Vercel.

## Data on hand (real, from `accounts`)
| City | Total | No phone | No website | Reachable | Status |
|---|---|---|---|---|---|
| Los Angeles | 979 | 540 | 472 | 439 | posted ✓ |
| Melbourne | 151 | 76 | 12 | 75 | next |
| Coimbatore | 524 | 355 | 425 | 169 | India — not targeted |
| Chennai | 152 | 21 | 60 | 131 | India — not targeted |
